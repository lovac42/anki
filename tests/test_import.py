import aqt
from anki import (__init__, cards, collection, consts, db, deck, decks, errors,
                  exporting, fields, find, hooks, lang, latex, media, model,
                  models, mpv, notes, sched, schedv2, sound, stats, statsbg,
                  stdmodels, storage, sync, tags, template, templates, utils)
from anki.importing import (__init__, anki2, apkg, base, csvfile, mnemo,
                            noteimp, pauker, supermemo_xml)
from anki.template import __init__, furigana, hint, template, view
from anki.utils import intTime, isMac, isWin
from aqt import (__init__, about, addcards, addons, browser, clayout,
                 customstudy, deck, deckbrowser, deckchooser, deckconf, decks,
                 downloader, dyndeckconf, editcurrent, editor, errors,
                 exporting, fields, importing, main, mediasrv, modelchooser,
                 models, overview, pinnedmodules, preferences, profiles,
                 progress, qt, reviewer, sound, stats, studydeck, sync,
                 tagedit, taglimit, toolbar, update, utils, webview)

if isWin:
    from aqt import winpaths







def test_import():
    pass
